export function trackEvent(eventName: string, properties?: Record<string, string | number | boolean>) {
  void eventName;
  void properties;
  // Connect Plausible, Vercel Analytics, Google Analytics, or another privacy-friendly provider here later.
}
