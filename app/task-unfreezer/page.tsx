'use client';
import { useState } from 'react';import { DisclaimerBox, Footer, Header, etsyListingUrl } from '@/components/site';
export default function Page(){const [task,setTask]=useState('');const [energy,setEnergy]=useState('Low');const [type,setType]=useState('Admin');const [done,setDone]=useState(false);return <><Header/><main><section><h1>Task feels frozen? Make it smaller.</h1><div className="tool"><label>What task are you avoiding?<input value={task} onChange={e=>setTask(e.target.value)} placeholder="clean my room"/></label><label>Energy<select value={energy} onChange={e=>setEnergy(e.target.value)}>{['Low','Medium','High','Overloaded'].map(x=><option key={x}>{x}</option>)}</select></label><label>Task type<select value={type} onChange={e=>setType(e.target.value)}>{['Admin','Cleaning','Study','Work','Creative','Message','Personal'].map(x=><option key={x}>{x}</option>)}</select></label><button className="btn" onClick={()=>setDone(true)}>Open Task Unfreezer</button>{done&&<article className="card"><h2>{task||'Your task'}</h2><p><b>Tiny start:</b> Put 5 visible items, words, or pieces into one simple container or list.</p><p><b>2-minute version:</b> Work only until the timer ends.</p><p><b>Dopamine pairing:</b> Play one song while you start.</p><p><b>Reward idea:</b> Tea break after the timer.</p><p><b>Done enough:</b> The basket is filled, one line is written, or 2 minutes passed.</p><p><b>PDF page suggestion:</b> Task Unfreezer Board for {type} · {energy} energy.</p><a className="btn small" href={etsyListingUrl}>Get the Printable on Etsy</a></article>}</div><DisclaimerBox/></section></main><Footer/></>}
